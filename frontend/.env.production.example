# Set these to the public API/WebSocket origins used by your deployment.
# Leave them unset when the frontend is served by the bundled nginx proxy.
# VITE_API_URL=https://your-api.example.com/api
# VITE_WS_URL=wss://your-api.example.com/api/ws

# Optional production scene tile CDN/object-store template
# VITE_SCENE_TILE_URL=https://your-cdn.example.com/streept/scene/{z}/{x}/{y}.json
