import {describe,expect,it} from 'vitest';import {assessBillboardSafety} from './billboardSafety';describe('billboardSafety',()=>{it('suppresses ads during critical maneuver',()=>expect(assessBillboardSafety({distanceToManeuverMeters:80,maneuverCritical:true,speedMps:10}).allowed).toBe(false));it('allows distant noncritical ads',()=>expect(assessBillboardSafety({distanceToManeuverMeters:300,maneuverCritical:false,speedMps:5}).allowed).toBe(true));});
