import {describe,expect,it} from 'vitest';import {allocateSceneAttention} from './sceneAttentionBudget';const p=(tier:any,score:number)=>({location:{lat:0,lng:0},tier,score,reason:'x'});describe('sceneAttentionBudget',()=>{it('keeps critical geometry inside budget',()=>expect(allocateSceneAttention([p('critical',1),p('critical',.9)],{critical:1,guidance:0,world:0,ambient:0})).toHaveLength(1));});
